10 REM Test ON TIMER(n) GOSUB - timer fires and calls subroutine
20 T% = 0
30 ON TIMER(0.05) GOSUB 100
40 SLEEP 0.2
50 IF T% > 0 THEN PRINT "TIMER FIRED" ELSE PRINT "TIMER DID NOT FIRE"
60 END
100 T% = T% + 1
110 RETURN
